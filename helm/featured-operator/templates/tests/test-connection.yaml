apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "featured-operator.fullname" . }}-test-connection"
  labels:
    {{- include "featured-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "featured-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
